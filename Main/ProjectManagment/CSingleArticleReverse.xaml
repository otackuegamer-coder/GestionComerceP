<UserControl x:Class="GestionComerce.Main.ProjectManagment.CSingleArticleReverse"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:GestionComerce.Main.ProjectManagment"
             mc:Ignorable="d" Height="86" Width="400">
    <UserControl.Resources>
        <!-- Article Item Style -->
        <Style x:Key="ArticleItemStyle" TargetType="Border">
            <Setter Property="Background" Value="White"/>
            <Setter Property="CornerRadius" Value="8"/>
            <Setter Property="Padding" Value="16"/>
            <Setter Property="Margin" Value="0,0,0,8"/>
            <Setter Property="BorderBrush" Value="#E5E7EB"/>
            <Setter Property="BorderThickness" Value="1"/>
        </Style>

        <!-- Reverse Button Style -->
        <Style x:Key="ReverseButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#EF4444"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Padding" Value="12,6"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="MinWidth" Value="80"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                CornerRadius="5"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" 
                                            VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#DC2626"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="#B91C1C"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>
    <Border Style="{StaticResource ArticleItemStyle}" Margin="0,0,0,8">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="0">
                <TextBlock Text="Service Standard XYZ" FontSize="15" FontWeight="SemiBold" Foreground="#1F2937" Margin="10,10,0,4" Name="ArticleName"/>
                <!--<Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Grid.Column="0" Text="Code: XYZ-456" FontSize="12" Foreground="#6B7280" Margin="0,0,16,0"/>
                    <TextBlock Grid.Column="1" Text="Qté: 1" FontSize="12" Foreground="#6B7280" Margin="0,0,16,0"/>
                    <TextBlock Grid.Column="2" Text="Prix: €45.95" FontSize="12" Foreground="#6B7280" Margin="0,0,16,0"/>
                    <TextBlock Grid.Column="3" Text="Total: €45.95" FontSize="12" Foreground="#059669" FontWeight="SemiBold"/>
                </Grid>-->
            </StackPanel>

            <Button Grid.Column="1" Content="Reverse" Style="{StaticResource ReverseButtonStyle}" Name="Reverse"
                Click="ReverseArticle_Click" Tag="XYZ-456"/>
        </Grid>
    </Border>
</UserControl>
