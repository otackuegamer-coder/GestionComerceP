<UserControl x:Class="GestionComerce.Main.Facturation.CreateFacture.CSingleRowClient"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:GestionComerce.Main.Facturation"
             mc:Ignorable="d" Height="71" Width="Auto">
    <Border Background="White" MouseLeftButtonDown="Border_MouseLeftButtonDown"
        CornerRadius="8"
        Padding="20,15"
        Margin="0,0,0,10"
        Cursor="Hand">
        <Border.Effect>
            <DropShadowEffect Color="#000000" 
                         Opacity="0.08" 
                         BlurRadius="15" 
                         ShadowDepth="2"/>
        </Border.Effect>

        <Border.Style>
            <Style TargetType="Border">
                <Style.Triggers>
                    <Trigger Property="IsMouseOver" Value="True">
                        <Setter Property="Background" Value="#F7FAFC"/>
                    </Trigger>
                </Style.Triggers>
            </Style>
        </Border.Style>

        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <!-- Client Name -->
            <TextBlock x:Name="ClientName"
                   Grid.Column="0"
                   Text="Nom du Client"
                   FontSize="15"
                   FontWeight="SemiBold"
                   Foreground="#2D3748"
                   VerticalAlignment="Center"/>

            <!-- Client Number -->
            <TextBlock x:Name="ClientNumber"
                   Grid.Column="1"
                   Text="+212 6XX XXX XXX"
                   FontSize="14"
                   FontWeight="Medium"
                   Foreground="#718096"
                   VerticalAlignment="Center"
                   Margin="15,0,0,0"/>
        </Grid>
    </Border>
</UserControl>
