<UserControl x:Class="GestionComerce.Main.Inventory.CSingleRowFamilly"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <UserControl.Resources>
        <!-- Delete Button Style -->
        <Style x:Key="ActionButtonStyle" TargetType="Button">
            <Setter Property="Width" Value="60"/>
            <Setter Property="Height" Value="25"/>
            <Setter Property="Margin" Value="2,0"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="6">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Opacity" Value="0.85"/>
                </Trigger>
                <Trigger Property="IsEnabled" Value="False">
                    <Setter Property="Opacity" Value="0.5"/>
                    <Setter Property="Background" Value="#E5E7EB"/>
                    <Setter Property="Foreground" Value="#9CA3AF"/>
                    <Setter Property="Cursor" Value="No"/>
                </Trigger>

            </Style.Triggers>
        </Style>

        <!-- TextBox Style -->
        <Style x:Key="RoleTextBoxStyle" TargetType="TextBox">
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="15,12"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border Background="{TemplateBinding Background}">
                            <ScrollViewer x:Name="PART_ContentHost"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <!-- Role Row -->
    <Border Background="White" 
            BorderBrush="#e9ecef" 
            BorderThickness="0,0,0,1"
            Height="50"
            CornerRadius="8">
        <Grid  MouseLeftButtonDown="MyGrid_MouseLeftButtonDown">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <!-- Role Name Input -->
            <TextBlock Grid.Column="0" 
         x:Name="FamillyName"
         Margin="10,10,55,-10" Grid.ColumnSpan="2" FontSize="18" FontWeight="Bold" FontFamily="Segoe UI Variable Text Semibold"/>

            <!-- Delete Button on the Right -->

            <Button Content="Edit" Background="#4A90E2" Style="{StaticResource ActionButtonStyle}" Click="UpdateButton_Click" Width="67" x:Name="UpdateButton" Grid.Column="1"/>
            <Button Content="Delete" Background="#E74C3C" Style="{StaticResource ActionButtonStyle}" Click="DeleteButton_Click" Width="67" x:Name="DeleteButton" Grid.Column="2"/>
        </Grid>
    </Border>
</UserControl>


