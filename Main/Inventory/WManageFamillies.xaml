<Window x:Class="GestionComerce.Main.Inventory.WManageFamillies"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Famille Management" 
        Height="700" Width="500"
        Background="Transparent"
        WindowStartupLocation="CenterScreen" 
        WindowStyle="None"
        AllowsTransparency="True">

    <Window.Resources>
        <!-- Modern Button -->
        <Style x:Key="ModernButton" TargetType="Button">
            <Setter Property="Background" Value="#3182CE"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Padding" Value="14,6"/>
            <Setter Property="Margin" Value="4"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="6">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#2C5282"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="#2A4365"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Opacity" Value="0.5"/>
                                <Setter Property="Background" Value="#E5E7EB"/>
                                <Setter Property="Foreground" Value="#9CA3AF"/>
                                <Setter Property="Cursor" Value="No"/>
                            </Trigger>

                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Secondary Button -->
        <Style x:Key="SecondaryButton" TargetType="Button" BasedOn="{StaticResource ModernButton}">
            <Setter Property="Background" Value="#E2E8F0"/>
            <Setter Property="Foreground" Value="#2D3748"/>
        </Style>

        <!-- Danger Button -->
        <Style x:Key="DangerButton" TargetType="Button" BasedOn="{StaticResource ModernButton}">
            <Setter Property="Background" Value="#E53E3E"/>
        </Style>

        <!-- Close Button Style -->
        <Style x:Key="CloseButton" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="#718096"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Width" Value="35"/>
            <Setter Property="Height" Value="35"/>
            <Setter Property="FontSize" Value="18"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                CornerRadius="6">
                            <ContentPresenter HorizontalAlignment="Center"
                                            VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#F7FAFC"/>
                                <Setter Property="Foreground" Value="#E53E3E"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="#EDF2F7"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Modern TextBox Style -->
        <Style x:Key="ModernTextBox" TargetType="TextBox">
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderThickness" Value="2"/>
            <Setter Property="BorderBrush" Value="#E2E8F0"/>
            <Setter Property="Height" Value="40"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Padding" Value="12,0"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="FontFamily" Value="Segoe UI"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border Background="{TemplateBinding Background}"
                        CornerRadius="6"
                        BorderThickness="{TemplateBinding BorderThickness}"
                        BorderBrush="{TemplateBinding BorderBrush}">
                            <!-- Let the content host expand fully -->
                            <ScrollViewer x:Name="PART_ContentHost"
                                  VerticalAlignment="Center"
                                  HorizontalAlignment="Stretch"
                                  Margin="{TemplateBinding Padding}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsKeyboardFocused" Value="True">
                                <Setter Property="BorderBrush" Value="#3182CE"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="BorderBrush" Value="#CBD5E0"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Panel Style -->
        <Style x:Key="ModernPanel" TargetType="Border">
            <Setter Property="Background" Value="White"/>
            <Setter Property="CornerRadius" Value="12"/>
            <Setter Property="Padding" Value="20"/>
            <Setter Property="Margin" Value="10"/>
        </Style>
    </Window.Resources>

    <Border Background="#F8FAFC" CornerRadius="10" BorderBrush="#E5E7EB" BorderThickness="1">
        <Grid>
            <!-- Close Button -->
            <Button Name="CloseButton"
                    Style="{StaticResource CloseButton}"
                    Content="×"
                    HorizontalAlignment="Right"
                    VerticalAlignment="Top"
                    Panel.ZIndex="1000"
                    Click="CloseButton_Click" Height="28" Width="31"/>

            <!-- Main Content -->
            <Grid Margin="20">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="2*" />
                    <ColumnDefinition Width="0" Name="ArticlesColumn"/>
                </Grid.ColumnDefinitions>

                <!-- Familles Section -->
                <Border Grid.Column="0" Style="{StaticResource ModernPanel}">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>

                        <TextBlock Text="Familles" 
                                   FontSize="22" 
                                   FontWeight="Bold" 
                                   Foreground="#1A202C"
                                   Margin="0,0,0,15"/>

                        <!-- Input and Add Button for Familles -->
                        <Grid Grid.Row="1" Margin="0,0,0,15">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="80"/>
                            </Grid.ColumnDefinitions>
                            <TextBox Name="FamilleInput" 
                                     Style="{StaticResource ModernTextBox}" 
                                     Grid.Column="0" 
                                     Margin="0,0,8,0" TextChanged="FamilleInput_TextChanged"/>
                            <Button Name="AddFamilleButton" 
                                    Style="{StaticResource ModernButton}" 
                                    Content="Add" 
                                    Grid.Column="1"
                                    Width="72" HorizontalAlignment="Left" Margin="4,4,0,4" Click="AddFamilleButton_Click"/>
                        </Grid>

                        <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
                            <StackPanel Orientation="Vertical" Margin="0,0,0,15" Name="FamilliesContainer"/>
                        </ScrollViewer>

                    </Grid>
                </Border>

                <!-- Articles Section -->
                <Border Grid.Column="1" Style="{StaticResource ModernPanel}">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>

                        <TextBlock Text="Articles" Name="ArticlesTitle"
                                   FontSize="22" 
                                   FontWeight="Bold" 
                                   Foreground="#1A202C"
                                   Margin="0,0,0,15" Grid.Row="0"/>

                        <!-- Input for Articles -->
                        <TextBox Name="ArticleInput" 
                                 Style="{StaticResource ModernTextBox}" 
                                 Grid.Row="1" 
                                 Margin="0,0,0,15" TextChanged="ArticleInput_TextChanged"/>

                        <!-- Replace your existing Grid with this modern version -->
                        <Border Grid.Row="2"
                                Background="#FAFBFC"
                                BorderBrush="#E2E8F0"
                                BorderThickness="0,0,0,1"
                                Padding="10,16,10,16">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="3*"/>
                                    <!-- Article Name -->
                                    <ColumnDefinition Width="2*"/>
                                    <!-- Fournisseur -->
                                    <ColumnDefinition Width="80"/>
                                    <!-- Quantity/Stock -->
                                    <ColumnDefinition Width="80"/>
                                    <!-- Actions -->
                                </Grid.ColumnDefinitions>

                                <TextBlock Grid.Column="0" 
                                           Text="ARTICLE" 
                                           VerticalAlignment="Center"
                                           FontWeight="SemiBold" 
                                           FontSize="11" 
                                           Foreground="#64748B"
                                           FontFamily="Segoe UI"/>

                                <TextBlock Grid.Column="1" 
                                           Text="FOURNISSEUR" 
                                           VerticalAlignment="Center"
                                           FontWeight="SemiBold" 
                                           FontSize="11" 
                                           Foreground="#64748B"
                                           FontFamily="Segoe UI"
                                           Margin="15,0,0,0"/>

                                <TextBlock Grid.Column="2" 
                                           Text="STOCK" 
                                           VerticalAlignment="Center"
                                           HorizontalAlignment="Center"
                                           FontWeight="SemiBold" 
                                           FontSize="11" 
                                           Foreground="#64748B"
                                           FontFamily="Segoe UI"
                                           Margin="30,0,0,0"/>

                                <TextBlock Grid.Column="3" 
                                           Text="ACTIONS" 
                                           VerticalAlignment="Center"
                                           HorizontalAlignment="Center"
                                           FontWeight="SemiBold" 
                                           FontSize="11" 
                                           Foreground="#64748B"
                                           FontFamily="Segoe UI"
                                           Margin="30,0,0,0"/>
                            </Grid>
                        </Border>

                        <ScrollViewer Grid.Row="3" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
                            <StackPanel Orientation="Vertical" Margin="0,0,0,15" Name="ArticlesContainer" />
                        </ScrollViewer>

                    </Grid>
                </Border>

            </Grid>
        </Grid>
    </Border>
</Window>